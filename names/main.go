package main

// if start by lowercase -> struct, interface, func just use inside package
type insidePackage struct {
}

// if start by uppercase -> struct, interface, func can use outside package
type OutsidePackage struct {
}

func main() {

}
